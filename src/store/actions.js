//import { SET_DATA } from './mutation-types';
//import router from "@/router";

/**
*
* @param { function } commit
* @param { string } data
*/
//export default {
/*
    setData({commit}, {data}) {
        commit(SET_DATA, {data});
    },
*/
/*
export function login({commit}, {loginName, loginPassword}) {
        commit("loginRequest");
        console.log(`Attempt to login ${loginName}:${loginPassword}`);
        // fake server auth
        setTimeout(() => {
            if (loginName === "bernspe" && loginPassword === "bernspe1234") {
                console.log("...succeeded");
                commit("loginSuccess", { loginName });
                router.push("/");
            } else {
                console.log("...failed");
                commit("loginFailure");
            }
        }, Math.floor(Math.random() * 2000));
    }
*/

